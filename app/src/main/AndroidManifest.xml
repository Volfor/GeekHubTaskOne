<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vtrifonov.geekhub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.TaskOneActivity"
            android:label="@string/task_1" />
        <activity
            android:name=".activities.MessageActivity"
            android:label="@string/task_1" />
        <activity
            android:name=".activities.TaskTwoActivity"
            android:label="@string/task_2" />
        <activity
            android:name=".activities.TaskThreeActivity"
            android:label="@string/task_3" />
        <activity
            android:name=".activities.TaskFourActivity"
            android:label="@string/task_4" />
    </application>

</manifest>
